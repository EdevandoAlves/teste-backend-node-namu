info:
  name: Update Program
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:3000/programs/4
  body:
    type: json
    data: |-
      {
        "name": "Yoga Matinal",
        "description": "antes de descansar",
        "category": "exercício",
        "duration_weeks": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
